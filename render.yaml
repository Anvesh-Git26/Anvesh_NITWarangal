services:
  - type: web
    name: bfhl-invoice-solver-docker
    env: docker
    dockerfilePath: Dockerfile
    plan: free
    numInstances: 1
    envVars:
      # Render will inherit GOOGLE_API_KEY from your Dashboard settings
      - key: GOOGLE_API_KEY
        sync: false
